extends layout 

block content 
	

	if plantfamily_plants.length 

		p #[strong Delete the following plants before attempting to delete this family:]

		div 
			h4 Plants 

			dl 
			each plant in plantfamily_plants 
				dt 
				a(href=plant.url) #{plant.name}
		
	else 
		
		div#deleteForm
			form(method="POST" action="")
				h1 Delete #{family.name}
				p Do you really want to delete this plant family?
				input(type="hidden", name="familyid", required=true, value=family._id)

				button(type="submit") Delete